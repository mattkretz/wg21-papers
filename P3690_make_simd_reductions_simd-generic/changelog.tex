\section{Changelog}
\begin{revision}
\item Add missing \code{auto} after \code{same_as<bool>} to the wording.
\item Fix \code{pow} call in an example, which was missing a second argument.
\end{revision}
