\section{Straw Polls}
\subsection{LEWG at Wrocław 2024}
\wgPoll{Remove wording that unconditionally allows calls to \code{gen} from the generator
  constructors to be unsequenced with respect to each other. At the same time, remove
\code{noexcept} from the constructors. (P3430R0 Section 4)}
{2 &9 &0 &0 &0}

\wgPoll{Reorder \code{binary_op} and \code{identity_element} as suggested by LWG and implemented in P1928R12.}
{3 &11 &0 &0 &0}
